pipeline {
     agent any
     stages {
         stage('Build') {
             steps {
                 echo 'Building...'
             }
             post {
                 always {
                     jiraSendBuildInfo site: 'https://firstjirasite.atlassian.net/', branch: 'FSR-1-add-to-cart-functionality'
                 }
             }
         }
     }
 }
